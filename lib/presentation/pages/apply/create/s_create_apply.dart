import 'package:flutter/material.dart';

class CreateApplyScreen extends StatelessWidget {
  const CreateApplyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
